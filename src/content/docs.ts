// 사용자 가이드/라이선스 같은 정적 문서를 별도 파일로 반입하지 않기 위해,
// 마크다운 텍스트를 여기 코드 안에 그대로 두고 main.js에 함께 번들합니다.
// 내용을 고칠 때는 이 문자열만 바꾸면 되고, 마크다운 문법(#, -, >)을 그대로 씁니다.

export const USER_GUIDE_MD = `# Intra Copilot 사용자 가이드

> 초안입니다. 기능이 늘어나면 이 문서도 함께 채워집니다.

## 시작하기

1. **설정 → Intra Copilot → LLM 연결** 탭에서 **서버 주소**(보통 \`/v1\`로 끝남)를 입력합니다. 서버가 인증을 요구하면 **API 키**도 입력합니다.
2. **[모델 목록 불러오기]**로 서버가 제공하는 모델을 불러와 하나를 고르고, **[연결 확인]**을 눌러 그 모델이 실제로 답하는지 확인합니다.
3. 왼쪽 리본의 로봇 아이콘으로 챗봇을 엽니다. 챗봇 머리줄에서도 모델을 바꾸고 연결 상태를 볼 수 있습니다. 상태 글자가 **연결됨**이면 준비된 것입니다.

앞으로 유사 노트 검색·링크와 Note Sweep 사용법이 이 문서에 추가될 예정입니다.

## 꼭 알아둘 점

### 대화 기록은 플러그인 폴더 안에 저장됩니다
- 저장 위치: \`.obsidian/plugins/intra-copilot/conversations/\`
- 플러그인을 **업데이트할 때는 main.js, manifest.json, styles.css 세 파일만 덮어쓰세요.** 폴더를 통째로 지우고 새로 넣거나 Obsidian에서 플러그인을 삭제하면 대화 기록도 함께 사라집니다.
- 대화 기록을 보관하고 싶다면 위 폴더를 따로 복사해 두세요.
- 스킬(\`.obsidian/plugins/intra-copilot/SKILL/\`)과 노트 수정 전 원본 백업(\`.obsidian/plugins/intra-copilot/backups/\`)도 같은 플러그인 폴더 안에 있으므로 같은 주의가 적용됩니다.
- 폴더 안의 \`index.json\`은 "지난 대화" 목록용 요약 파일입니다. 목록이 실제와 다르게 보이면 이 파일만 지우세요 — 다음에 목록을 열 때 대화 파일들로 다시 만들어집니다.

### API 키 보관
- API 키는 설정 파일(\`.obsidian/plugins/intra-copilot/data.json\`)에 그대로 저장됩니다. 볼트를 통째로 복사하거나 공유할 때는 이 파일이 함께 가지 않도록 주의하세요.

### 서버 연결
- 이 플러그인은 설정에 입력한 LLM 서버 주소로만 요청을 보냅니다.
- 서버가 요청을 다른 주소로 넘기려 해도(리다이렉트) 따라가지 않습니다. (http 주소일 때. https 주소는 확인 필요)

### 답변 속 링크와 이미지
- 답변 속 링크는 보안 정책상 클릭해도 열리지 않습니다. 주소가 옆에 글자로 보이고, 누르면 주소가 클립보드에 복사됩니다.
- 외부 주소의 이미지도 불러오지 않고, 🖼 표시와 함께 주소만 보여줍니다.
- 답변 속 HTML 태그는 실행하지 않고 글자 그대로 보여줍니다. (줄바꿈·굵게 같은 간단한 서식 태그만 그대로 적용됩니다.)

### 답변 속 코드는 실행되지 않습니다
- Dataview 같은 다른 플러그인이 설치되어 있어도, 챗봇 답변 속 코드블록(dataviewjs 등)이나 인라인 쿼리는 실행되지 않고 코드 그대로 보입니다.

### 노트를 챗봇에게 읽히기 — 입력칸 위의 "칩"
챗봇에게 전송되는 노트는 **입력칸 위에 칩으로 올라와 있는 것뿐입니다.** 칩에 없는 노트는 전송되지 않습니다.

**지금 열려 있는 노트는 자동으로 칩에 올라옵니다.**
- 노트를 열면 그 노트가 칩에 나타나고, 테두리가 둘러져 다른 칩과 구별됩니다. 그 상태로 질문하면 **노트 내용이 함께 전송됩니다.**
- 보내고 싶지 않으면 질문하기 전에 칩의 **×**를 누르세요. 다른 노트로 옮기기 전까지는 다시 올라오지 않습니다.
- 노트가 아닌 파일(PDF 등)을 열거나 아무것도 열지 않으면 이 칩은 사라집니다.
- 이 칩이 곧 **고칠 수 있는 노트**이기도 합니다(아래 "수정을 노트에 반영하기" 참고).

**@로 다른 폴더·노트를 더 읽히기**
- 입력칸에 **@**를 치면 이 볼트의 폴더·노트 목록이 뜹니다. 이어서 글자를 칠 때마다 목록이 좁혀집니다.
- **↑↓**로 고르고 **Enter**(또는 Tab)를 누르거나, 마우스로 클릭하면 선택됩니다. **Esc**로 목록을 닫습니다.
- 목록 맨 위의 **볼트 전체**와 **현재 노트**(지금 보고 있는 노트)로 바로 지정할 수도 있습니다.
- @로 고른 칩은 지울 때까지 남아서, 이어지는 질문마다 그 내용이 함께 전송됩니다. 칩의 ×(또는 입력칸이 비어 있을 때 Backspace)로 해제합니다.
- @로 여러 노트를 읽히더라도 **고쳐지는 노트는 열려 있는 노트 하나뿐**입니다. 읽는 범위는 넓힐 수 있지만 바뀌는 파일은 항상 하나입니다.

**공통**
- 노트 내용은 **보낼 때마다 그 순간의 노트를 새로 읽어서** 붙입니다. 대화 기록 파일에는 노트 내용이 아니라 경로와 보낸 분량만 저장됩니다.
- 질문 말풍선의 "노트 N개 · N자 첨부"를 누르면 **어떤 노트가 실제로 전송됐는지** 목록을 펼쳐 볼 수 있습니다.
- 보낼 수 있는 분량은 고급 설정의 **노트 자료 최대 글자 수**(기본 8000자)까지입니다. 넘치는 노트는 빼고 보내며, 질문 말풍선에 "일부 생략"이라고 표시됩니다. 폴더가 크면 폴더 대신 필요한 노트만 지정하세요.

### 챗봇이 제안한 수정을 노트에 반영하기

**고칠 수 있는 노트는 "지금 열려 있는 노트" 하나뿐입니다.**
- 노트를 열면 그 노트가 입력칸 위에 **칩으로 자동으로 들어옵니다.** 그 상태로 "○○를 고쳐 줘"라고 하면 됩니다.
- @로 다른 폴더·노트를 더 지정해서 **참고 자료로 읽히는 것은 얼마든지 가능**하지만, **실제로 고쳐지는 것은 열려 있는 노트 하나**입니다. 여러 노트를 읽히더라도 바뀔 수 있는 파일은 항상 하나라서, 어느 노트가 고쳐지는지 헷갈릴 일이 없습니다.
- 기준은 **질문을 보낸 시점**에 열려 있던 노트입니다. 답변을 읽다가 다른 노트로 옮겨도 그 답변의 [적용]은 원래 노트에 적용됩니다(카드에 그 노트 이름이 적혀 있습니다).
- 현재 노트를 보내고 싶지 않으면 칩의 ×를 누르세요. 다른 노트로 옮기기 전까지는 다시 들어오지 않습니다.

**카드 읽는 법**
- 카드 맨 위에 챗봇이 **왜 그렇게 고치려 하는지 근거**가 적혀 있습니다. 이 이유를 먼저 읽고 적용할지 정하세요.
- 카드에서 **빨간 줄(-)**은 노트에서 **지울 내용**, **초록 줄(+)**은 **넣을 내용**입니다. 바뀌지 않는 줄이 길면 "… 바뀌지 않은 N줄 생략"으로 접힙니다.
- **[적용]을 누르기 전에는 노트가 전혀 바뀌지 않습니다.** 카드는 제안일 뿐이고, 반영할지는 항상 사용자가 정합니다.
- 카드 위의 **노트 이름을 누르면** 그 노트가 열립니다. 적용하기 전에 실제 노트를 확인할 때 쓰세요.
- 적용하면 버튼이 **[되돌리기]**로 바뀝니다. 되돌리기는 **그때 고친 부분만** 원래대로 돌리므로, 그 뒤에 직접 고친 다른 부분은 그대로 남습니다.
- 적용 직전의 노트 원본은 플러그인 폴더의 \`backups/\` 안에 파일로 보관됩니다(최근 200개까지). 되돌리기가 실패할 만큼 노트가 많이 바뀐 경우, 이 파일을 열어 내용을 직접 되살릴 수 있습니다.
- 카드가 흐리고 **[적용] 버튼이 없다면** 그 아래 적힌 이유 때문입니다. 대부분 다음 중 하나입니다.
  - **열려 있던 노트가 아님**: 챗봇이 다른 노트를 고치자고 한 경우입니다. 그 노트를 열고 다시 요청하세요.
  - **고칠 원문을 찾지 못함**: 노트가 그새 바뀌었거나, 챗봇이 원문을 조금 다르게 옮겨 적었습니다. 챗봇에게 다시 물어보세요.
  - **여러 곳에 있음**: 똑같은 대목이 노트에 여러 번 나와서 어디를 고칠지 알 수 없습니다. "앞뒤 줄을 더 넣어서 다시 제안해 줘"라고 하세요.
  - **노트가 없음**: 경로가 틀렸거나 노트가 옮겨졌습니다.
- 여러 군데를 한 번에 고치는 제안이면 카드가 여러 개 나옵니다. **카드마다 따로** 적용하거나 넘어갈 수 있습니다.
- 제안이 둘 이상이면 답변 맨 위에 **"노트 수정 제안 N개"**와 **[모두 적용]**이 표시됩니다. [모두 적용]은 한 번 누르면 경고로 바뀌고, 4초 안에 **한 번 더 눌러야** 실제로 적용됩니다(적용할 수 없는 제안은 건너뜁니다). 하나씩 확인하는 편이 안전하므로, 내용을 이미 훑어본 경우에만 쓰세요.
- 적용을 마친 카드는 **한 줄로 접힙니다.** 왼쪽 세모(▸)를 누르면 다시 펼쳐 내용을 확인할 수 있습니다.
- 대화를 다시 불러오면 이미 적용한 카드는 "적용함"으로 표시된 채로 보입니다.
- 답변을 받는 동안에는 수정 제안 부분이 "(노트 수정 제안을 쓰는 중…)"으로 보이다가, 다 받으면 카드로 바뀝니다.

### /로 스킬 불러 쓰기
- 자주 하는 작업 지시(예: "이 노트의 링크를 정리해 줘")를 **스킬**로 저장해 두고, 입력칸에 **/**를 쳐서 불러 씁니다. 목록 사용법은 @와 같습니다.
- 고른 스킬은 입력칸 위에 보라색 칩으로 표시되고, 보내면 풀립니다(한 번 쓰고 끝). @로 지정한 폴더·노트 칩은 그대로 남습니다.
- 스킬만 고르고 입력칸을 비운 채 보내도 됩니다. 글을 쓰면 지시문과 함께 전송됩니다.
- 스킬은 **설정 → Intra Copilot → 스킬** 탭에서 추가·편집·삭제합니다.
- 스킬은 플러그인 폴더의 \`SKILL/\` 폴더에 스킬마다 .md 파일 하나로 저장됩니다. 메모장으로 직접 고쳐도 되고(고친 뒤 스킬 탭의 [목록 새로고침]), 파일을 복사해 동료와 나눌 수 있습니다. 파일 모양:

\`\`\`
---
name: 노트 링크 정리
description: 목록에 보이는 한 줄 설명
---

여기부터 끝까지가 모델에게 보내는 지시문입니다.
추가 요청: {{input}}
\`\`\`

- 지시문에 \`{{input}}\`을 쓰면 그 자리에 입력칸의 글이 들어갑니다. 없으면 입력한 글이 지시문 뒤에 붙습니다.

### 보낸 메시지를 고쳐서 다시 보내기
- 내 말풍선에 마우스를 올리면 나오는 ✏️(수정)를 누르면, 그 메시지의 글·스킬·@ 대상이 입력칸으로 돌아옵니다.
- 수정하는 동안 고치는 메시지는 점선 테두리로, 그 아래 대화는 흐리게 보입니다. 이때는 아직 아무것도 지워지지 않았고, **[취소]**나 **Esc**를 누르면 원래대로 돌아갑니다.
- 고쳐서 보내면 **그 메시지부터 아래 대화가 지워지고**(대화 기록 파일에서도) 새 메시지로 이어집니다. 서버에는 수정한 지점 이전의 대화만 가므로, 방향이 틀린 질문·답변이 대화 기록 한도를 차지하지 않습니다.
- 지운 대화는 되돌릴 수 없습니다. 남겨 두고 싶다면 수정 대신 [새 대화]로 시작하세요.

### 답변을 기다리는 동안
- 기본적으로 답변은 서버가 만드는 대로 글자가 **차례로 나타납니다**(스트리밍). 다 받은 뒤에 서식(굵게·목록·코드)이 적용된 모습으로 바뀝니다.
- 서버가 스트리밍을 막아 두어 문제가 생기면, 고급 설정의 **답변 스트리밍**을 꺼서 예전처럼 한 번에 받을 수 있습니다.
- 기다리는 동안에도 다음 질문을 미리 써 둘 수 있습니다. [중지]를 누르면 기다리기를 멈추고 질문을 입력칸에 되돌려 줍니다. (서버는 이미 받은 질문의 답변을 끝까지 만들 수 있습니다.)

### 챗봇 여는 방법
- 왼쪽 리본의 로봇 아이콘을 누르거나, 명령 팔레트(Ctrl+P)에서 "챗봇 열기"를 실행합니다. 설정 → 단축키에서 원하는 키를 지정할 수도 있습니다.
- Obsidian을 다시 켜면 마지막으로 보던 대화가 다시 열립니다.
`;

export const LICENSE_MD = `# Intra Copilot 라이선스 및 정책

> 이 문서는 초안입니다. 실제 배포 전에 내용을 검토하고 다듬어야 합니다.

- 사내 폐쇄망 환경 전용으로 만들어졌습니다. 데스크톱용 Obsidian에서만 동작하며, 모바일 앱에서는 동작하지 않습니다.
- 모든 통신은 설정에서 지정한 LLM 서버와만 이루어집니다. 서버로 보내는 내용은 다음뿐입니다.
  - 사용자가 채팅에 입력한 글, 그리고 함께 보내는 이전 대화와 기본 지시문(시스템 프롬프트)
  - 사용자가 /로 고른 스킬의 지시문
  - 챗봇 입력칸 위에 **칩으로 올라와 있는** 폴더·노트의 내용(설정한 최대 글자 수 이내). 칩에는 @로 직접 고른 것과, **지금 열려 있는 노트가 자동으로** 올라옵니다.
  - 연결 확인용 테스트 문장, 모델 목록 요청
- **칩에 없는 노트는 전송되지 않습니다.** 노트를 열어 둔 채 질문하면 그 노트가 칩에 있으므로 함께 전송됩니다. 보내고 싶지 않으면 질문하기 전에 칩의 ×를 누르세요(다른 노트로 옮기기 전까지 다시 올라오지 않습니다).
- **노트를 고치는 경우**는 사용자가 챗봇 답변의 수정 카드에서 **[적용] 버튼을 직접 누를 때뿐**이며, 고칠 수 있는 노트는 **그 질문을 보낼 때 화면에 열려 있던 노트 하나**로 제한됩니다. 플러그인이 스스로 노트를 바꾸거나 지우는 일은 없습니다.
- 지금 열려 있는 노트는 챗봇 입력칸 위에 칩으로 자동 표시되며, 그 상태로 질문을 보내면 내용이 전송됩니다. 보내지 않으려면 칩의 ×로 지울 수 있습니다.
  - 고치기 직전의 노트 원본은 플러그인 폴더의 \`backups/\`에 보관되며(최근 200개), 이 파일도 볼트 안에 있으므로 외부로 전송되지 않습니다.
- 라이선스: 검토 필요 — 현재 저장소 기본값은 0-BSD입니다.
- 동료에게 배포하기 전에 정보보안/IT 담당 부서의 사전 확인을 권장합니다.
`;
